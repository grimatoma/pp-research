# Godot 4 ignores
.godot/
.import/
export.cfg
# NOTE: export_presets.cfg IS committed — it is project config (the Web export
# preset), needed by the GitHub Pages deploy workflow. Do not ignore it.

# Mono/.NET (unused, GDScript project) — harmless guards
.mono/
data_*/
mono_crash.*.json

# Builds
/build/
/dist/
*.exe
*.pck
*.zip

# OS / editor noise
.DS_Store
Thumbs.db
*.tmp
*~
.vscode/
.idea/
